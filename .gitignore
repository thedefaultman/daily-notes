# The user's real config is written to ~/.claude/daily-notes/config.json at setup
# time, never into the repo. But if someone copies it here for editing, keep it out.
config.json
config.local.json

# OS / editor noise
.DS_Store
*.swp
*~
.idea/
.vscode/

# Test scratch
/tmp-test/
*.log
